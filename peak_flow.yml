before_script:
  - npm install
  - (cd example && npm install)
  - (cd example && npx expo export --platform web --output-dir ../dist)
script:
  - npm run typecheck
  - SYSTEM_TEST_HOST=dist npm test
  - npx expo-doctor
